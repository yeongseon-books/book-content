# Revisions and traffic splitting — where Envoy weights come from

> Azure Container Apps Deep Dive series (3/6)

Azure Container Apps makes rollout mechanics look gentler than they are.

You update an image.
ACA creates a revision.
You move some percentage of traffic.
The app keeps serving.

That surface is clean because the product hides several lower-level steps.
An immutable revision has to be created.
That revision has to become active.
Ingress has to know which revisions are eligible for traffic.
And the final request path has to apply weights somewhere concrete.

That final step is the reason for this episode.
If traffic is split 80/20 in the portal or ARM template, where does that number actually matter?

The short answer is: at the Envoy routing layer, via weighted upstream selection.
The long answer starts with revision immutability.

---

## A revision is an immutable runtime snapshot

Microsoft's revisions documentation is explicit on the important part.
Revisions are immutable snapshots of a container app.

That one sentence is the operational heart of ACA.

It means the running unit you shift traffic to is not a mutable deployment slot.
It is a distinct snapshot produced from a specific revision-scope template.

![A revision is an immutable runtime snapshot](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-01-a-revision-is-an-immutable-runtime-snaps.en.png)
This explains several product behaviors that otherwise look unrelated.

- Changing the image creates a new revision.
- Changing scale rules creates a new revision.
- Changing app-scope settings such as traffic configuration does not create a new revision.
- Rolling back often means re-pointing traffic to an older immutable revision rather than mutating the latest one.

Once you internalize immutability, the rest of the rollout model becomes consistent.

---

## Revision-scope versus application-scope changes

ACA divides changes into two buckets.

Revision-scope changes create a new revision.
Application-scope changes affect the app surface without minting a new revision.

This is one of the most useful distinctions in the entire product.

![Revision-scope versus application-scope changes](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-02-revision-scope-versus-application-scope.en.png)
Microsoft documents the `properties.template` area as revision-scope and the `properties.configuration` area as application-scope.

Revision-scope examples include:

- Container image updates
- Container configuration updates
- Scale rule changes
- Revision suffix changes

Application-scope examples include:

- Ingress configuration
- Traffic splitting rules
- Revision mode
- Labels
- Secrets
- Registry credentials
- Dapr settings

This partition is why traffic movement can happen independently of building a new revision.

---

## Single revision mode and multiple revision mode are not just UI toggles

The revision mode changes how ingress interprets app history.

In single revision mode, ACA tries to keep the rollout simple.
Once the new revision is ready, traffic moves to it and old revisions are deprovisioned automatically.

In multiple revision mode, several active revisions can coexist and receive traffic concurrently.
That is the mode where canary and blue-green become natural.

![Single revision mode and multiple revision mode are not just UI toggles](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-03-single-revision-mode-and-multiple-revisi.en.png)
The deep-dive lesson is this.
The mode is not merely a release preference.
It changes the set of revisions Envoy is allowed to treat as upstream candidates.

---

## Labels and weights solve different routing problems

ACA offers two routing ideas that often get mixed together.

1. Traffic weights on the main app URL.
2. Labels that expose a stable direct URL for one revision.

Weights distribute requests probabilistically across active revisions.
Labels pin a separate URL to one revision at a time.

![Labels and weights solve different routing problems](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-04-labels-and-weights-solve-different-routi.en.png)
That distinction matters operationally.

- Use weights when you want gradual exposure on the production app URL.
- Use labels when you want deterministic direct access for testing or staging.

Labels are not weighted routing.
They are stable named pointers.

---

## What the traffic configuration looks like from the product surface

ACA exposes traffic rules as application-scope ingress configuration.
The schema is intentionally compact.

You can point traffic at:

- `latestRevision: true`
- a named revision
- a label

Each entry has a weight.
The weights must sum to 100.

```json
"configuration": {
  "ingress": {
    "external": true,
    "targetPort": 80,
    "traffic": [
      {
        "revisionName": "myapp--blue",
        "weight": 80
      },
      {
        "revisionName": "myapp--green",
        "weight": 20
      }
    ]
  }
}
```

This configuration is small because it is product-facing.
It does not show you the ingress proxy internals that will honor those percentages.

That hidden step is where Envoy enters the story.

---

## From revision weight to Envoy weight

Envoy terminology can be confusing here, so pin the vocabulary first.

In Envoy, a cluster means an upstream destination grouping.
It does not mean a Kubernetes cluster.

That matters because revision traffic splitting is naturally expressed as weighted cluster selection.

The upstream Envoy route schema defines weighted clusters in the route configuration API.
That is the right conceptual anchor for ACA traffic splitting.

![From revision weight to Envoy weight](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-05-from-revision-weight-to-envoy-weight.en.png)
ACA itself is closed-source, so you do not see the exact product adapter code.
But the combination of Microsoft's traffic-splitting docs and Envoy's weighted-cluster model is enough to explain the mechanism accurately.

---

## Why the weight belongs at the ingress layer and not inside the app

Once multiple revisions are active, something has to pick the destination before the request enters the user container.
That something is the ingress routing layer.

If the user app had to decide, the request would already be at one revision, which defeats the point.

![Why the weight belongs at the ingress layer and not inside the app](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-06-why-the-weight-belongs-at-the-ingress-la.en.png)
The weight has to exist before the service hop.
That is why episode 6 will trace the full path as client to load balancer to Envoy to service to pod.

For this episode, the key point is narrower: traffic weights are ingress routing data, not application runtime logic.

---

## Zero-downtime rollout depends on readiness, not just on revision creation

Microsoft's revisions documentation is careful here.
In single revision mode, traffic does not move to the latest revision until the new revision is ready.

Ready means more than "a revision object exists."

- Provisioning succeeded.
- Replicas scaled up to the expected count.
- Startup and readiness probes passed.

![Zero-downtime rollout depends on readiness, not just on revision creation](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-07-zero-downtime-rollout-depends-on-readine.en.png)
So when people say ACA provides zero-downtime deployment in single revision mode, the deeper statement is that the control plane waits for a readiness threshold before re-pointing ingress.

---

## Multiple revision mode turns rollout into routing math

Once you enable multiple revisions, rollout stops being a binary switch.
It becomes a routing table you control.

Common patterns map directly onto weight changes.

- Blue-green: 100/0, test, then 0/100.
- Canary: 95/5, then 80/20, then 50/50, then 0/100.
- A/B test: hold two or more revisions live at fixed percentages.

![Multiple revision mode turns rollout into routing math](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-08-multiple-revision-mode-turns-rollout-int.en.png)
The elegant part is that the revision remains immutable while the exposure changes around it.
That separation is exactly what you want in a controlled rollout system.

---

## `latestRevision: true` is convenient, but it changes who owns the switch

Traffic configuration can say "send 100% to the latest revision" instead of naming a specific revision.

That is useful for rapid iteration.
It is also a shift in control.

When you point at `latestRevision: true`, you are telling ACA to keep the app URL attached to the newest ready revision.
When you point at explicit revision names, you are taking that decision back.

![`latestRevision: true` is convenient, but it changes who owns the switch](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-09-latestrevision-true-is-convenient-but-it.en.png)
This distinction often marks the difference between a fast-moving dev setup and a tightly controlled production rollout.

---

## A hidden but important split: active revision versus traffic-receiving revision

In multiple revision mode, a revision can be active without receiving production traffic from the main app URL.

That is an important mental model.

Active means the revision can run.
Traffic weight decides whether the main app URL sends requests there.
Labels can still expose it directly even if the main URL gives it zero percent.

![A hidden but important split: active revision versus traffic-receiving revision](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-10-a-hidden-but-important-split-active-revi.en.png)
This is how staging-style validation can happen without touching the main production path.

---

## Revision weight and scale are related but not identical systems

It is tempting to think 20% of traffic means 20% of replicas.
ACA does not promise that.

Traffic weight is ingress routing policy.
Replica count is scale policy per revision.

They influence each other through load, but they are not the same control loop.

![Revision weight and scale are related but not identical systems](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-11-revision-weight-and-scale-are-related-bu.en.png)
That is why a 5% canary can still need more than one replica, and why a 50/50 split does not mechanically force equal replica counts if the revisions differ in performance or concurrency behavior.

---

## Where Envoy's weighted cluster model matters most

Envoy's route API and router implementation matter for one reason here.
They make weighted upstream selection a first-class proxy concern.

That lets ACA keep rollout control at ingress while leaving revisions immutable.

The platform-specific step you do not see is how ACA names and manages the upstreams for revisions.
The proxy concept is still standard.

![Where Envoy's weighted cluster model matters most](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-12-where-envoy-s-weighted-cluster-model-mat.en.png)
This is the cleanest explanation for the user's question, "where do the weights come from?"
They come from app-scope ACA ingress configuration and become ingress routing weights applied to revision upstreams.

---

## The rollback story is better because revisions stay immutable

If a canary goes wrong, you do not need to rebuild the previous version.
You move traffic back.

That sounds obvious, but it depends on two design choices holding together.

1. Old revisions remain addressable.
2. Traffic policy is separate from revision creation.

![The rollback story is better because revisions stay immutable](https://raw.githubusercontent.com/yeongseon/tech-blog/e8dca42/assets/azure-aca-deep-dive/03/03-13-the-rollback-story-is-better-because-rev.en.png)
The speed of that rollback is one of the strongest practical benefits of the ACA revision model.

---

## What closed-source ACA hides, and what it does not

You cannot inspect ACA's private control-plane code that translates app config into Envoy config.
That part is Microsoft-owned.

But you do not need to guess wildly.

You have three strong anchors.

1. Microsoft Learn defines revision modes and traffic rules.
2. Microsoft Learn defines which changes are revision-scope versus application-scope.
3. Envoy's pinned route API defines the weighted-cluster concept that fits the behavior.

That is enough to reason accurately without pretending the product is open-source where it is not.

---

## Episode 3 wrap

Here is the compact model.

> A revision in ACA is an immutable runtime snapshot. Traffic splitting is an application-scope ingress decision that chooses which active revisions are exposed on the app URL. The percentages ultimately belong to the Envoy routing layer, where revision upstreams can be treated as weighted destinations.

Once you have that model, canary and blue-green stop looking like magic portal features.

---

## Where this fits in the series

This episode connected the environment boundary from part 2 to the immutable rollout unit inside it. The next part moves from rollout policy to autoscaling policy, following how ACA scale rules create KEDA-driven behavior per revision. After that, the series drops into the Dapr sidecar and finally comes back to Envoy for the full ingress request path that actually applies the weights described here.

---

<!-- toc:begin -->
## In this series

- [ACA architecture — what Microsoft layered on a hidden Kubernetes](./01.md)
- [Environment internals — the network, observability, and Dapr scope boundary](./02.md)
- **Revisions and traffic splitting — where Envoy weights come from (current)**
- KEDA inside ACA — what a scale rule actually creates (upcoming)
- Dapr sidecar internals — the Go process that lives next to your container (upcoming)
- The Envoy ingress path — how the first request reaches your container (upcoming)

<!-- toc:end -->

---

## References

**Primary sources**
- [`Envoy` route components at `v1.30.0`](https://github.com/envoyproxy/envoy/blob/v1.30.0/api/envoy/config/route/v3/route_components.proto)
- [`Envoy` router implementation at `v1.30.0`](https://github.com/envoyproxy/envoy/blob/v1.30.0/source/common/router/config_impl.cc)

**Secondary sources**
- [Update and deploy changes in Azure Container Apps](https://learn.microsoft.com/en-us/azure/container-apps/revisions)
- [Traffic splitting in Azure Container Apps](https://learn.microsoft.com/en-us/azure/container-apps/traffic-splitting)
- [Ingress in Azure Container Apps](https://learn.microsoft.com/en-us/azure/container-apps/ingress-overview)

**Related series**
- [Azure Container Apps 101](https://github.com/yeongseon/tech-blog/blob/e8dca42/azure-aca-101/en)
- [Azure AKS Deep Dive](https://github.com/yeongseon/tech-blog/blob/e8dca42/azure-aks-deep-dive/en)
- [Azure Functions Deep Dive](https://github.com/yeongseon/tech-blog/blob/e8dca42/azure-functions-deep-dive/en)

Tags: Container Apps, KEDA, Dapr, Envoy
